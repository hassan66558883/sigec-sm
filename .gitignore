/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# electron-builder output (SIGEC-SM-Setup-*.exe etc.) — genere par `npm run
# dist:win`, jamais commite. NB: /build lui-meme N'EST PLUS ignore : ce
# dossier contient maintenant des assets source reels (build/icon.ico),
# convention par defaut d'electron-builder (buildResources), pas une sortie
# de build Next.js (qui va dans .next/, deja ignore ci-dessus).
/release

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env
.env.test
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma (regenerated automatically via postinstall: "prisma generate")
/src/generated/
